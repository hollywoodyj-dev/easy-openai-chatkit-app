## Wisewave Chat – QA Handoff

**Last updated:** 2026-04-03 (Lumen rerun A/C pass; hedge-after-practical prompt tightening for rerun B watchpoint)  
**Environment:** Next.js app at `/chat` (Option B – backend turn API + DB persistence)

### 1. High‑level status

- **Backend persistence**:  
  - `POST /api/chat/session` creates a DB `Conversation` (session_id = conversation.id).  
  - `POST /api/chat/turn` saves user + assistant messages and returns `assistant_message`.  
  - `GET /api/chat/messages?session_id=...` returns all saved messages for that conversation.  
  - `GET /api/chat/continuity?session_id=...` returns the latest **stable**, continuity-eligible insight for the **active inner thread** of that conversation only (V3); without `session_id` the response is **`insight: null`** (no conversation-wide or cross-chat strip).  
  - `GET /api/chat/sessions` returns a per‑user list of recent conversations with a topic preview.

- **Identity / isolation**:  
  - Authenticated users: identified via `Authorization: Bearer <token>` (JWT).  
  - Anonymous users: identified via a long‑lived cookie; separate from logged‑in identities.  
  - `/chat` also scopes `sessionStorage` keys by token, so switching accounts in the same browser does **not** share history.

- **LLM behavior / profile**:  
  - Backend uses a Wisewave‑style system prompt from either `OPENAI_CHAT_SYSTEM_PROMPT` or `OPENAI_CHAT_SYSTEM_PROMPT_FILE`.  
  - Incremental conversation summarization is implemented to maintain continuity without sending full history every turn.

### 2. `/chat` UI status

- **Working now**:
  - On load, `/chat` creates or resumes a session and loads messages.  
  - Left sidebar shows **Conversations** list from `/api/chat/sessions`, auto‑refreshed:  
    - on initial load,  
    - after starting a **New** chat,  
    - after each successful message turn.  
  - Active conversation is highlighted in the list.  
  - Message roles are labeled as **“You”** (user) and **“Wisewave”** (assistant).  
  - Error states (e.g. failure to create session or send message) show inline banners with simple recovery actions.

- **Known behaviors to keep in mind**:
  - Topics in the sidebar are derived from the **first user message** in each conversation (truncated to 60 chars).  
  - Long‑running chats rely on periodic summaries for context; very old turns may be represented only via the summary.

### 3. Open questions / good QA targets for Lumen

- **Milestone J (micro-shift):** Formal pass — **`docs/HC_OS_V1_Milestone_J_Lumen_QA_Plan.md`**; **results + closure stance** — **`docs/HC_OS_V1_Milestone_J_Lumen_QA_Results.md`** (**2026-03-30**). Flag **`ENABLE_J_MICROSHIFT`**; debug **`debug_milestone_j_*`**; **`microshift_cue`** when emitted. **Lumen closure read:** **behaviorally healthy** on host; **do not** claim full evidence-closure or file the remaining item as a confirmed bug. **Proven:** `milestone_j_microshift_v1`, kill switch (local), utilitarian (**`Summarize…`** path), **H-conflict**, **E/F `presence_risk`**, basic J emit, **EN/ZH parity provisionally acceptable**. **Open proof gap only:** hosted **`conflict:i_active`** (E often preempts I → `recurrence_overlap_e` / `presence_risk`; §2c hard to force when turn 1 stays eligible). **Follow-up** only if stricter closure required. Probes: **`docs/HC_OS_V1_Milestone_J_Nova_Hosted_Probes_Lumen.md`**. Utilitarian: **`looksUtilitarianOrFactual`** includes summarize/outline/paraphrase (J + I).
- **Local dev `503` `db_unreachable` on `/api/chat/session`:** Option B **`/chat`** needs **`DATABASE_URL`** (PostgreSQL) in **`.env.local`** and schema applied (**`npx prisma migrate deploy`** or **`db push`**). See **README §3** and **`.env.example`**. Not a Milestone J code defect.
- **Profile correctness (production vs local)**  
  - Confirm in **local** and **production** that the assistant consistently behaves like the Wisewave profile (not “generic GPT”).  
  - Verify env vars are set correctly in each env:  
    - Either `OPENAI_CHAT_SYSTEM_PROMPT` (inline text), or  
    - `OPENAI_CHAT_SYSTEM_PROMPT_FILE` pointing to a prompt file path (e.g. `prompts/wisewave-system.txt`).

- **History & session UX**  
  - Confirm that after sending at least one message, the conversation appears in the sidebar with an appropriate topic.  
  - Confirm switching accounts (different JWT tokens) shows **isolated** histories in the sidebar and message view.  
  - Confirm that refreshing `/chat` keeps you in the same active conversation for that account (via `sessionStorage`).  
  - Confirm that starting **New** creates a fresh conversation and the old one remains accessible in the sidebar.

- **Error handling**  
  - Simulate network or API failures (e.g. disable network briefly) and confirm:  
    - Message send failures show a clear error and allow retry.  
    - Session creation failures show a clear error and the **Try again** link works.

### 4. How Lumen should report new findings

When Lumen has new QA results or bugs:

1. Add a dated entry at the bottom of this file, e.g.:
   - `2026‑02‑10 – Found: switching accounts sometimes reuses the same sidebar list. Steps: ... Expected: ... Actual: ...`
2. Clearly mark whether it’s:
   - **Blocker for internal beta**, or  
   - **Nice‑to‑have / polish**, or  
   - **Question / clarification**.
3. Save and commit as part of the QA branch so the dev agent can pick it up from here.

### 5. New findings from Lumen (2026-03-13)

- **2026-03-13 16:15 Sydney — Nice-to-have / polish (product trust)**
  - **Found:** `/chat?token=invalid-test-token` still renders the normal chat shell instead of surfacing a clear auth/token error state.
  - **Steps:** Open `http://127.0.0.1:3000/chat?token=invalid-test-token` in a fresh browser session.
  - **Expected:** User should see a clear invalid/stale token state or be redirected to an appropriate recovery path.
  - **Actual:** Standard chat UI rendered, and the frontend created a token-scoped `sessionStorage` key: `chat_session_id:invalid-test-token`.
  - **Notes:** This does **not** by itself prove backend account access is granted. It does show a trust/clarity gap because a bad token currently looks more valid than it should.

- **2026-03-13 16:25 Sydney — Nice-to-have / polish (mobile discoverability)**
  - **Found:** Mobile `/chat` session/history discoverability appears broken by an implementation mismatch.
  - **Steps:** Open authenticated token `/chat` in iPhone 14 emulation, then tap the visible `Conversations` control.
  - **Expected:** Tapping `Conversations` should reveal a mobile conversation list / drawer / panel.
  - **Actual:** No visible history drawer/panel appeared; user stayed in the plain chat view with composer only.
  - **Implementation clue:** The full conversation sidebar markup still exists in the DOM, but it is hidden on mobile via `hidden sm:flex`, while the visible mobile `Conversations` button does not appear to reveal that hidden list.
  - **Notes:** Desktop/token-path baseline remains green; this is a mobile-specific discoverability issue, not a core persisted-chat regression.

### 6. Implementation follow-up (2026-03-13) — what changed

Both 2026-03-13 QA items above have been implemented in the codebase:

- **Invalid-token UX**
  - New API: `GET /api/chat/auth-check`. With `Authorization: Bearer <token>`, it returns **200** for a valid token and **401** for an invalid/expired token. With no Bearer header it returns 200 (anonymous allowed).
  - `/chat` now calls this endpoint when a `token` query param is present; on **401** it shows an **“Invalid or expired sign-in link”** screen with `Sign in again` → `/login` and `Continue without account` → `/chat` (no token), and does **not** create a session or write to `sessionStorage` for that bad token.

- **Mobile Conversations drawer**
  - On viewports below the `sm` breakpoint, tapping **“Conversations”** in the chat header opens a left-side drawer with backdrop, the conversation list (same as the desktop sidebar), a **Close** control, and **New conversation**. Choosing a conversation or starting a new one closes the drawer and updates the main chat view.

### 7. Lumen retest results (2026-03-13)

- **2026-03-13 16:35 Sydney — Retest result: PASS (invalid-token UX)**
  - Retested `http://127.0.0.1:3000/chat?token=invalid-test-token` in a fresh browser session.
  - **Observed:** Dedicated `Invalid or expired sign-in link` screen rendered with the expected recovery actions (`Sign in again`, `Continue without account`).
  - **Observed:** No token-scoped `sessionStorage` key was created for the invalid token.
  - **Conclusion:** The earlier invalid-token trust/clarity issue is resolved in local QA.

- **2026-03-13 16:35 Sydney — Retest result: PASS (mobile Conversations drawer)**
  - Retested authenticated token `/chat` in iPhone 14 emulation.
  - **Observed:** Tapping `Conversations` opened a mobile drawer with backdrop, Close control, `New conversation`, and the real conversation list.
  - **Observed:** After the drawer loaded, conversation items were visible. Tapping `New conversation` closed the drawer and returned to the normal chat view after session loading.
  - **Conclusion:** The earlier mobile session-discoverability issue is resolved in local QA.

- **2026-03-13 16:40 Sydney — Sanity check: PASS (profile correctness on token `/chat`)**
  - Retested on the authenticated token `/chat` path with the prompt: `In one sentence, who are you and what kind of assistant are you?`
  - **Observed reply:** `I'm Wisewave, an AI companion here to support your self-awareness and personal growth through thoughtful conversation.`
  - **Conclusion:** This is positive local evidence that the active `/chat` assistant profile is aligned with the intended Wisewave persona rather than a generic assistant voice.
  - **Caution:** A separate plain `/chat` automation session drifted onto `/embed` and hung on `Loading assistant session...`, so that older surface should not be used as the primary trust signal for the persisted `/chat` path.

- **2026-03-13 16:45 Sydney — Partial switching/isolation check: PASS (token vs anonymous contexts)**
  - Retested authenticated token `/chat` and plain anonymous `/chat` in separate clean browser sessions after today's implementation changes.
  - **Observed (token path):** token-scoped sessionStorage key (`chat_session_id:<token prefix>`) and the richer token-side conversation history were present.
  - **Observed (anonymous path):** `chat_session_id:anon` was used and the visible history was separate/minimal rather than reusing the token-side conversation list.
  - **Conclusion:** This is additional live evidence that account-context separation remains intact after today's changes.
  - **Limitation:** This is not yet a full logout/login switching proof across two distinct authenticated accounts; it is a token-vs-anonymous isolation check.

### 8. Milestone H observation — passive vs benchmark metrics (2026-03-24)

- **Do not mix:** Treat **benchmark-set** suppression ratios (filtered by `benchmarkSet` in the internal observation tool) as **different** from **passive observation** ratios (unfiltered or `benchmarkSet=__passive__`).
- **Record:** Lumen + Wisewave end-of-day benchmark summary and locked reporting rule: **`docs/HC_OS_V1_Milestone_H_Lumen_Wisewave_Benchmark_End_of_Day_Summary.md`**. **Combined 24–25 interpretation** (closure posture, H4/H3/H1/H5, Tree/Nova brief): **`docs/HC_OS_V1_Milestone_H_Wisewave_Combined_Report_2026-03-24_to_2026-03-25.md`**. **v4 benchmark rerun** (vs v3): **`docs/HC_OS_V1_Milestone_H_Lumen_Wisewave_Benchmark_v4_Results_2026-03-25.md`**.
- **2026-03-25 — Nova:** **`milestone_h_v6`** deployed in repo (`lib/wisewave-milestone-h-micro-awareness.ts`): family-targeted H3 redundancy suppression — suppress H3 when the main reflection already carries the needed value; treat generic `default` H3 phrasing as danger patterns when redundant; **H4** templates and routing **unchanged**. **Lumen should retest:** confidence-25 and regression-14 rows with remaining revise families (replay/rumination, reply anxiety, prove/earn residual); confirm `debug_milestone_h_build_marker` = **`milestone_h_v6`** on hosted after deploy.
- **2026-03-25 — Wisewave:** **Agreed.** v6 is the strongest Milestone H result so far and supports a *serious closure conversation*. Next step: run **one final narrow confirmation pass** (include the remaining rest/guilt edge case), then decide whether to move from “continue stabilization” to **closure consideration / confirmation pass**.
- **2026-03-26 — Nova (post-H containment):** **`milestone_h_v7`** suppression tightening after Day 1 + Day 2 post-H sample packs: (1) **hard-kill factual/utilitarian drafting** (rewrite/summarize/polish/meeting-notes/email), (2) **H1 main-reflection-sufficiency** suppression (avoid additive tails in medium reflective turns), (3) **medium-signal downgrade** (H1 no longer default-allowed). **Lumen retest:** rerun Day 1 + Day 2 and confirm **no utilitarian H**, fewer medium reflective H1, fewer “removal-better”, and `debug_milestone_h_build_marker` = **`milestone_h_v7`** on hosted.
- **2026-03-26 — Nova (post-H containment follow-up):** **`milestone_h_v8`** narrow patch for remaining medium H1 emissions after hosted v7 retest (`h-d01-006`, `h-d01-007`, `h-d02-006`, `h-d02-007`): added explicit **moment-level activation** requirement for medium-band H1 (EN/ZH). If a medium reflective turn lacks clear in-the-moment activation substrate, H1 suppresses with `h1_medium_requires_moment_activation`. **H4 routing unchanged.** **Lumen retest:** rerun Day 1 + Day 2 sentinels and confirm marker **`milestone_h_v8`** on hosted.
- **2026-03-26 — Nova (post-H narrow cleanup):** **`milestone_h_v9`** targets remaining hosted survivors (`h-d01-006`, `h-d01-007`, `h-d02-006`) by adding a medium-band H1 suppression gate when the main reflection already captures pressure/perfection/rest-worth or ambient bracing/vigilance movement (`h1_medium_main_reflection_capture`). Intent: reduce additive visible layer in medium reflective EN/ZH while preserving existing high-signal and H4 behavior.
- **2026-03-26 — Nova (post-H cross-kind containment):** **`milestone_h_v10`** addresses v9 reroute behavior (`h-d02-006` shifted from H1 to H5) by adding a medium-band **cross-kind substitution block**: when main reflection already captures pressure/perfection/rest-worth or ambient bracing/vigilance movement, suppress non-H4 awareness surfacing with `h_medium_cross_kind_substitution_block`. Target sentinels: `h-d01-006`, `h-d01-007`, `h-d02-006`. **H4 preserved.**
- **2026-03-26 — Nova (post-H medium-boundary correction):** **`milestone_h_v11`** shifts from patching outputs to tightening **admissibility**: for medium-band pressure/perfection/rest-worth/bracing-vigilance shapes, awareness now defaults to suppress unless stronger admissibility is present (`moment-level activation` + reflective structure + durable insight pattern). Suppression reason: `h_medium_boundary_default_suppress`. Targets unchanged: `h-d01-006`, `h-d01-007`, `h-d02-006`; cross-kind substitution should remain blocked.
- **2026-03-26 — Nova (post-H lane-agnostic doctrine patch):** **`milestone_h_v12`** implements Wisewave medium-band doctrine across all lanes (H1/H3/H4/H5): medium-band is now **default suppress unless necessity is proven** (`h_medium_lane_agnostic_default_suppress`), and global main-reflection sufficiency suppresses awareness across lanes (`h_medium_main_reflection_sufficient_global`). Targets: `h-d01-006`, `h-d01-007`, `h-d02-006`, plus `h-d02-005` no H4 reopen.
- **2026-03-26 — Nova (post-H ZH medium parity tightening):** **`milestone_h_v13`** narrows remaining ZH medium-band survivors (`h-d01-006`, `h-d02-006`) by requiring stricter Chinese live-activation proof before medium-band admissibility can pass (`h_medium_zh_activation_not_strong_enough`). Intent: preserve v12 lane-agnostic doctrine while removing additive H1 admissions on ZH pressure/rest-permission wording.
- **2026-03-26 — Nova (post-H final ultra-narrow cleanup):** **`milestone_h_v14`** adds a residual **exception-deny** gate for the last ZH medium-band H1 pocket (`h-d01-006`, `h-d02-006`): when user+insight both match pressure/perfection/rest-permission family and main reflection is already materially sufficient, suppress H1 with `h1_zh_medium_residual_exception_deny`. No lane expansion; keep cross-lane suppression discipline.
- **2026-03-28 — Nova (post-H Day 4/5 ZH H4 corridor):** **`milestone_h_v15`** — surgical ZH-only H4 admission tightening per **`docs/post-h-nova-patch-brief-day4-day5-2026-03-28.md`**. When `kind=H4`, user message has CJK, and the turn is in the **comparison / keep-up / perfection-pressure** corridor (`constant_pressure_keep_up` family or narrow ZH regex on user+insight), suppress with **`h4_zh_comparison_keepup_corridor_main_reflection_sufficient`** if **`isMainReflectionMateriallySufficientGlobal(insight)`** already holds (additive easing H4 treated as removable). **Non-goals respected:** no global H4 retune, no EN path change, factual/vague-source untouched. **Lumen retest:** minimum suppress `h-d05-003`, `h-d05-004`, `h-d05-006`; preferred also `h-d04-003`, `h-d04-004`. **Regression:** EN Day 5 comparison rows, `h-d05-009` `vague_source`, factual rows, Day 3 cleanliness.
- **2026-03-28 — Lumen (hosted `milestone_h_v15` retest):** ZH H4 corridor is **real** (`h-d05-006` suppresses with `h4_zh_comparison_keepup_corridor_main_reflection_sufficient`); regressions **clean** (EN Day 5 comparison suppressed, `h-d05-009` still `vague_source`, factual suppressions held). **Still narrow:** high-signal ZH survivors **`h-d05-003`, `h-d05-004`, `h-d04-003`, `h-d04-004`** still emit H4. **Direction:** one more **surgical widen** of the same corridor (keep `main_reflection_sufficient` gate; do not retune EN / factual / vague-source). **Day 3 note:** snapshot not perfectly flat (`h-d03-003` emitted H1) — **recheck Day 3** after the next patch.
- **2026-03-28 — Nova (post-H ZH H4 corridor v16):** **`milestone_h_v16`** — widens **`isZhH4ComparisonKeepUpAdmissionCorridor`** only: **`和别人比`**, **`越比越`**, 看别人/别人 + 更顺/顺, **否定自己**, prove-pressure (**反复想证明** / **说不出来…在证明**), slight cold → not-good-enough (**冷淡…怀疑/不够**, **怀疑自己…不够好**, **一点点冷淡**). Same suppression reason **`h4_zh_comparison_keepup_corridor_main_reflection_sufficient`**; same **`isMainReflectionMateriallySufficientGlobal`** gate. **Lumen retest:** confirm the four ZH survivors suppress; repeat regression pack + **Day 3** (`h-d03-003`); hosted marker **`milestone_h_v16`**.
- **2026-03-28 — Lumen (hosted `milestone_h_v16`):** five-row target set **3/5 suppressed** (was 1/5 on v15); EN comparison, **`vague_source`**, and factual suppressions still clean. **Remaining H4:** **`h-d05-004`** (comparison spiral / 越比越…停不下来) and **`h-d04-003`** (slight cold → 我不够好-style collapse). **`h-d03-003`** still H1 — treat as separate Day-3 track, not this corridor. **Stance:** if one more code touch, **micro-pass only** — ZH-only, H4-only, same **`main_reflection_sufficient`** gate.
- **2026-03-28 — Nova (post-H ZH H4 corridor v17):** **`milestone_h_v17`** — last micro-widen of **`isZhH4ComparisonKeepUpAdmissionCorridor`**: explicit **越比越…停不下**, **拿自己…和别人比 / 跟人比**, **忍不住…(比|比较|对比)**; cold-collapse bridges **是不是不够好**, **哪怕…冷淡**, **冷淡…不够好** (long span). Same reason + gate as v15–v16. **Lumen retest:** **`h-d05-004`**, **`h-d04-003`**; regression pack unchanged; **`h-d03-003`** optional (H1 / different issue). Hosted marker **`milestone_h_v17`**.
- **2026-03-28 — Lumen (hosted `milestone_h_v17`):** corridor stayed bounded; **no more blind regex widening**. **`h-d05-003`**, **`h-d04-004`** suppress with `h4_zh_comparison_keepup_corridor_main_reflection_sufficient`; EN comparison, **`vague_source`**, factual still clean. **Residuals:** **`h-d05-004`**, **`h-d04-003`** still H4; **`h-d05-006`** reopened as **H1** (was suppressed on earlier builds). **Read:** lane interaction / admission hierarchy — kind uses **English-heavy insight regex**; thin hosted insight can fail **`isMainReflectionMateriallySufficientGlobal`** while the ZH user turn already states the movement; misrouted **H1** never saw the corridor block (`kind === "H4"` only).
- **2026-03-28 — Nova (post-H ZH corridor v18 — routing / sufficiency):** **`milestone_h_v18`** — (1) **`isZhCorridorUserUtteranceAlreadyCarriesRemovableMovement`** ORs with global main-sufficient when in corridor (user-utterance witness for thin/English insight). (2) Same suppression reason applies when **`kind === "H1"`** and corridor + sufficiency (align with H4 neighborhood; **H3/H5 untouched**). **Not** further corridor regex expansion. **Lumen retest:** **`h-d05-004`**, **`h-d04-003`**, **`h-d05-006`** + prior regression pack; optional **`debug_milestone_h_kind`** vs **`debug_milestone_h_suppressed_reason`** to confirm H1-path suppressions. Marker **`milestone_h_v18`**.
- **2026-03-29 — Lumen (hosted `milestone_h_v18` — v18 acceptance):** v18 **looks real** on its intended target. **Read:** Day 4/5 ZH comparison-corridor fix is **bounded and genuine**; the **routing / sufficiency** change did the job — **not** “one more regex” in effect. **Retest target set (confirmed right):** **`h-d05-004`**, **`h-d04-003`**, **`h-d05-006`** + usual EN comparison / **`vague_source`** / factual controls; key verify **`h-d05-006`** does **not** escape as H1. **Stance:** treat v18 as **successful closure for the Day 4/5 comparison corridor** — **do not widen that corridor further.**
- **2026-03-29 — Lumen (Post-H Day 6 full sample, hosted `milestone_h_v18`):** **`docs/post-h-day6-full-sample-result-2026-03-29.md`**. Reviewed 12; H appeared 5 / suppressed 7 (58.3%); **revise 5**; **EN clean**, **factual clean**. **Read:** **not** the same Day 4/5 corridor problem — new residual pocket is **ZH responsibility / over-carry / vigilance**; common pattern: **main reflection already sufficient** but **removable H still survives**. **Survivors:** `h-d06-003`, `h-d06-004` (ZH high reflective → H1); `h-d06-006` (ZH medium reflective → H1); `h-d06-009` (ZH medium mixed → H1); **`h-d06-012`** (ZH low-signal mixed → **H4**) — **most concerning** (low-signal opens H4 easing; hard to defend under duplication review). **Nova if next patch:** target **duplication / main-reflection-already-sufficient suppression** for this Day 6 ZH pocket — **not** further comparison-corridor widening.
- **2026-03-30 — Nova (Post-H Day 7 patch — `milestone_h_v19`):** Lumen brief: ZH **H1** survivors on **long** turns where **`insight_candidate` already names** avoidance↔heavier, return-after-avoid, rational “wise” vs fear/discomfort, or not-really-released put-down — **duplication**, not corridor expansion. **Change:** `isZhInsightAvoidanceReturnStructureAlreadyNamed(insight)` + suppress **`kind === "H1"`** with **`h1_zh_avoidance_return_insight_sufficient`** (CJK in insight only; **no** EN / factual path changes). **Lumen retest:** Day 7 survivors **`h-d07-003`–`009`** → expect **0–1** H survivors; re-run EN + factual controls; confirm **no** Day 6 low-signal H4 regression. **Marker:** **`milestone_h_v19`**.
- **2026-03-30 — Lumen (hosted rerun on `milestone_h_v19`) → diagnosis:** Day 7 rerun still shows the same ZH survivors **`h-d07-003` / `h-d07-004` / `h-d07-006` / `h-d07-009`** (no net improvement). Diagnosis: the v19 ZH-only guard was too brittle because on hosted survivors the debug `insight_candidate` is **EN fallback-generic** (no CJK), so the ZH-surface-only check never activates. **What to patch:** make the v19 guard robust to EN fallback insight_candidate (or gate on ZH user text + detect avoidance/return/wise-vs-fear via EN keywords). **Next marker:** **`milestone_h_v20`**.
- **2026-03-30 — Lumen (post-H Day 7 rerun on `milestone_h_v20`) → clean pass:** 12 reviewed; H appeared `0` / H suppressed `12` (suppression ratio **100%**). Prior survivors **`h-d07-003` / `h-d07-004` / `h-d07-006` / `h-d07-009`** are now suppressed with **`h1_zh_avoidance_return_insight_sufficient`**. EN containment and factual suppression still hold; no new Day 7 low-signal H4 pocket appears. Recommended next step: use **Day 8** regression/comparison checks.
- **2026-03-30 — Lumen (post-H Day 8 rerun on `milestone_h_v20`) → bounded new pocket:** 12 reviewed; H appeared **3** / H suppressed **9** (suppression ratio **75%**). All survivors are **H1**: `h-d08-004`, `h-d08-006`, `h-d08-009`. Day 7 lane is fixed on v20; Day 8 reveals a new bounded ZH H1 pocket consistent with **time-pressure / false urgency / anticipatory urgency**. Likely next patch direction: ZH H1 suppression when main reflection already clearly names urgency / false urgency / anticipatory time pressure (still-have-time vs already-feels-too-late; anxiety mistaken for urgency). Recommended next regression/comparison: **Day 9**.
- **2026-03-30 — Lumen (hosted Day 7 rerun follow-up on `milestone_h_v20`) → bounded reopen:** 12 reviewed; H appeared **2** / H suppressed **10**. Survivors are **`h-d07-004`** and **`h-d07-009`** (both ZH H1). Current read: same broad ZH H1 duplication / main-reflection-already-sufficient class; **not** a new corridor class. Under Phase 2 detection-only governance, this item is logged as a board/record update only (no automatic optimization trigger).
- **2026-03-30 — Lumen (hosted Day 8 rerun follow-up on `milestone_h_v20`) → clean-set confirmation after transport noise:** initial pass had `POST /api/chat/session` **503** on 7 cases; Lumen reran failed cases and merged results before judgment. Final set: **12 reviewed / 12 HTTP 200**; H appeared **3** / H suppressed **9**. Survivors: `h-d08-004` (**H1**), `h-d08-006` (**H4**), `h-d08-009` (**H1**). Read: confirms existing bounded ZH urgency / false-urgency / anticipatory-urgency pocket on Day 8; **not** a new corridor and **not** a broad reopen. Under Phase 2 detection-only governance, this is logged as board/handoff observation evidence, not an automatic patch request.
- **2026-03-30 — Lumen (hosted Day 9 rerun follow-up on `milestone_h_v20`) → clean-set confirmation after transport noise:** initial pass had transport noise on `h-d09-001` (`POST /api/chat/session` **503**); Lumen reran that case before judgment and rerun returned **200** with H suppressed. Final set: **12 reviewed / 12 HTTP 200**; H appeared **3** / H suppressed **9**. Survivors: `h-d09-004` (**H1**), `h-d09-009` (**H1**), `h-d09-012` (**H1**). Read: bounded Day 9 ZH-side residual pocket confirmed; **not** a new corridor and **not** a broad reopen. Factual/utilitarian suppression remains intact in this pass. Under Phase 2 detection-only governance, this is logged as board/handoff observation evidence, not an automatic patch request.
- **2026-03-30 — Lumen (hosted Day 10 rerun follow-up on `milestone_h_v20`) → broader residual spread, clean transport:** clean first pass **12 reviewed / 12 HTTP 200**; H appeared **4** / H suppressed **8**. Survivors: `h-d10-004` (**H1**), `h-d10-006` (**H4**), `h-d10-009` (**H5**), `h-d10-012` (**H1**). Read: Day 10 is not clean and shows broader residual spread than Day 8/9 because survivors are not confined to one subtype. Logged as Phase 2 detection-only board/handoff evidence, **not** an automatic patch request.
- **2026-03-30 — Lumen (hosted Day 11 rerun follow-up on `milestone_h_v20`) → narrower bounded residual pocket, clean transport:** clean first pass **12 reviewed / 12 HTTP 200**; H appeared **3** / H suppressed **9**. Survivors: `h-d11-003` (**H1**), `h-d11-004` (**H1**), `h-d11-006` (**H1**). Read: Day 11 is not clean but residual shape is narrower than Day 10 (bounded H1 pocket vs multi-subtype spread). Logged as Phase 2 detection-only board/handoff evidence, **not** an automatic patch request.
- **2026-03-30 — Lumen (hosted Day 12 rerun follow-up on `milestone_h_v20`) → bounded subtype-heavy lane, clean transport:** clean first pass **12 reviewed / 12 HTTP 200**; H appeared **4** / H suppressed **8**. Survivors: `h-d12-003` (**H4**), `h-d12-004` (**H4**), `h-d12-006` (**H4**), `h-d12-009` (**H1**). Read: Day 12 is not clean and shows a bounded H4-dominant residual pocket with one H1; narrower than broad multi-subtype reopen but distinct enough to log as subtype-heavy lane. Logged as Phase 2 detection-only board/handoff evidence, **not** an automatic patch request.
- **2026-03-30 — Lumen (hosted Day 13 rerun follow-up on `milestone_h_v20`) → bounded H1 pocket, clean transport:** clean first pass **12 reviewed / 12 HTTP 200**; H appeared **3** / H suppressed **9**. Survivors: `h-d13-003` (**H1**), `h-d13-004` (**H1**), `h-d13-006` (**H1**). Read: Day 13 is not clean but remains a bounded H1 residual pocket; similar to Day 11 and narrower than Day 10 broad subtype spread or Day 12 H4-dominant pocket. Logged as Phase 2 detection-only board/handoff evidence, **not** an automatic patch request.
- **2026-03-30 — Lumen (hosted Day 14 rerun follow-up on `milestone_h_v20`) → bounded H1 pocket, clean transport:** clean first pass **12 reviewed / 12 HTTP 200**; H appeared **3** / H suppressed **9**. Survivors: `h-d14-003` (**H1**), `h-d14-004` (**H1**), `h-d14-006` (**H1**). Read: Day 14 is not clean but remains a bounded H1 residual pocket; closely matches Day 11/13 shape rather than Day 10 broad subtype spread or Day 12 H4-dominant pocket. Logged as Phase 2 detection-only board/handoff evidence, **not** an automatic patch request.
- **2026-03-30 — Lumen (Phase 2 first-pass language grounding read, hosted Day 10-12 outputs) → PASS WITH WATCHPOINTS:** 22 observation units (locked unit: `1 user input -> 1 final assistant response`). Severity tally: Level 0 = **8**, Level 1 = **10**, Level 2 = **4**, Level 3 = **0**. Locked bar check: `>=80% Level 0/1` **PASS** (`18/22 = 81.8%`), `0 Level 3` **PASS**, `<=2 Level 2` **NOT MET**. Main read: direction is broadly right, but not yet flat-clean by locked success definition; residual risk is mainly ZH optional/additional-line territory (H1/H4-style cue lines and low-signal/factual extra soft lines adding interpretation load). Pattern is not primarily vocabulary drift; concentration is Type B explanation drift + Type C optional-line drift + Type D low-signal compensation drift. Board posture: detection-only language observation evidence, not immediate rewrite request. Reference: `docs/HC_OS_V1_Phase_2_Language_Grounding_First_Pass_2026-03-30.md`.
- **2026-03-30 — Wisewave/Nova (Unified Drift operational chain lock):** Dashboard -> Logging -> Decision flow is now formally locked in Unified Drift docs for H/I/J operational mode (Detection Only / No Expansion, post-J closure): **System observes -> Lumen records -> Nova logs -> Tree decides -> OctopusMind governs** (system never directly acts). Added explicit role gates and approval boundaries: Nova is state recorder only (no hidden optimization/re-interpretation), dashboard is decision-support only, Tree is sole rollback decision point, and rule/scoring/rollback logic changes require Tree + OctopusMind. Also locked "reuse existing H/I/J QA cases + unified template layer" (no new drift test type by default). References: `docs/HC_OS_V1_Unified_Drift_Detection_System_v1.md`, `docs/HC_OS_V1_Unified_Drift_Detection_Use_Guide_v1.md`.
- **2026-03-30 — Wisewave/Nova (Phase 2 addendum lock, post A-J):** Added formal execution addendum for **Phase 2 — Reality Integration** with **Detection Only / No Expansion** operating posture. Locks end-to-end chain (**User -> Wisewave -> Lumen -> Nova -> Tree -> OctopusMind**), module discipline (live observation, layer integrity monitoring, failure-pattern mapping), onboarding constraint protocol (locked wording), trigger conditions (Tree review + rollback evaluation thresholds), role lock (Lumen recorder / Nova state memory / Tree controller / OctopusMind governance), and explicit prohibited behaviors during observation phase. Reference: `docs/HC_OS_V1_Phase_2_Post_A_to_J_Operational_Execution_Addendum.md`.
- **2026-03-30 — Wisewave/Lumen (post-H threshold governance clarification, Day 7-14):** Locked classification is now **Tree awareness = YES**, **Tree emergency = NO**, **rollback trigger = NOT established**, framing = **Continuous Pattern Drift review visibility**. Reason: Day 7-14 shows repeated bounded residual recurrence (governance visibility warranted), but escalation conditions are not met (no Presence drift, Guidance drift, Authorship drift, or override-style signal). Clarification locked: repeated residual recurrence alone does **not** justify system change; escalation requires movement toward noticeability, removal-preferred behavior, or high-risk drift classes. Operational next steps (detection-only): (1) map all Day 7-14 survivor units into unified drift ledger, (2) mark unit-level Pattern Drift = YES where applicable, (3) verify threshold windows (**3 consecutive** or **4 of last 6**), (4) keep Tree posture in review mode only (no patching, no rollback, no optimization momentum).  
- **2026-04-02 — Lumen (new governance artifact: survivor-only unified drift ledger, hosted Day 7-14 reruns):** Added **`docs/HC_OS_V1_Phase_2_Post_H_Day7_to_Day14_Survivor_Unified_Drift_Ledger_2026-04-02.md`** as a **narrow evidence base** under Phase 2 detection-only posture. Scope is intentionally limited to **25 survivor units only** (not full all-units rolling-window governance math). Ledger-aligned summary in the artifact: **Pattern Drift = YES (25/25)**, **Remove H -> better? = YES (19/25)**, high-risk classes (**Presence/Guidance/Authorship**) = **0**, severity held at **Level 1** across rows, dominant lane = recurring **ZH H1 main-reflection-sufficient** pocket, with Day 12 as a bounded **H4-dominant** watch lane. Governance read remains unchanged: **Tree awareness = YES**, **Tree emergency = NO**, **rollback trigger not established**. This artifact supports **Continuous Pattern Drift review visibility** only; it is **not** standalone justification for rollback, patching, or optimization momentum.  
- **2026-04-02 — Lumen (Phase 2 narrow confirmation pass request, detection-only):** Prepare/log a **narrow confirmation pass** to verify whether the known weak cluster tightened after targeted revise, without reopening broad Phase 2 evaluation. Locked scope: ZH additional-line cases (especially H1/H4-style cue shapes), low-signal/factual cases, and main-reflection-already-sufficient cases where an extra line still appears. Locked unit: **`1 user input -> 1 final assistant response`**. Per-case fields: Conceptual Language Present; Interpretation Required; Process-based; Body-match; Remove abstraction better; Severity (L0-L3); Optional line should have been suppressed. Sample guidance: **10-14 units** (6 ZH additional-line, 4 low-signal/factual, 2-4 main-reflection-sufficient). Success criteria: no L3; L2 count lower than prior weak-cluster pattern; most low-signal/factual cases do not receive unnecessary extra soft lines; ZH H1/H4 additional lines feel less interpretive/distance-creating; main reflection remains intact while optional-line suppression tightens. Output format locked: overall result + totals + main findings + recommendation. Board posture reminder: this is a **narrow confirmation pass** only, not a broad rewrite or architecture reopen.  
- **2026-04-02 — Nova (Lumen-ready narrow confirmation-pass packet prepared/logged):** Created **`docs/HC_OS_V1_Phase_2_Narrow_Confirmation_Pass_Packet_2026-04-02.md`** as the execution packet for Lumen judgment (not Nova judgment). Packet contains: locked role split, locked unit (`1 user input -> 1 final assistant response`), locked weak-cluster scope, Nova-prepared 12-unit narrow set (ZH H1/H4 additional-line anchors + low-signal/factual controls + main-reflection-sufficient anchors), fill-in per-case judgment table, and locked Lumen output format (overall result / totals / findings / recommendation). Governance framing remains detection-only and narrow-scope; no broad rewrite or architecture reopen.
- **2026-04-02 — Lumen (Phase 2 narrow confirmation-pass result, detection-only):** Completed the 12-case narrow packet judgment. **Overall result: Partial improvement.** Totals: **L0=3, L1=6, L2=3, L3=0**; optional lines present **11/12**; optional lines judged should-have-been-suppressed **6/12**. Read: no broad conceptual regression; main reflections remain mostly process-close; no Level 3/systemy-language signal. Known weak cluster is not yet cleanly resolved: ZH additional-line behavior remains the main issue (especially H1/H4-style shapes when main reflection already lands), with some low-signal/factual-adjacent rows still carrying extra soft lines without added clarity. Board posture remains locked: keep Phase 2 direction, no broad rewrite, no architecture reopen. Recommended next step: one more narrow suppression tighten on optional/additional lines only for ZH H1/H4 + low-signal/factual/near-factual rows. Reference: **`docs/HC_OS_V1_Phase_2_Narrow_Confirmation_Pass_Results_2026-04-02.md`**.
- **2026-04-02 — Tree/Lumen/Nova (implementation direction lock + narrow tighten applied):** Tree confirmed posture remains **Pass with watchpoints**: core Phase 2 direction is good, closure not yet justified, one more narrow revise justified. Locked implementation guide: if main reflection already lands and extra line mainly softens/echoes/lightly reframes, suppress it. Nova applied a narrow H-layer tighten in **`lib/wisewave-milestone-h-micro-awareness.ts`** (**`milestone_h_v21`**), adding targeted suppression for ZH H1/H4 optional-line emission when main reflection is already sufficient and lane is low-signal/factual-adjacent (`h_zh_optional_line_main_reflection_sufficient`). No broad rewrite or architecture changes. Next loop prepared under same detection-only posture: **`docs/HC_OS_V1_Phase_2_Narrow_Recheck_Packet_2026-04-02_v2.md`**.
- **2026-04-02 — Lumen (hosted narrow recheck on `milestone_h_v21`) → suppression target succeeded, possible new regression flagged:** narrow suppression target succeeded (**12 reviewed / 12 HTTP 200; H appeared 0; H suppressed 12**). However, recheck exposed a possible hosted response-quality/language-routing regression: multiple affected ZH rows returned generic English fallback-style uncertainty framing (e.g., “There seems to be a lot of uncertainty in that...”, “There may be a little more room here than it first seems.”). Current read: technical suppression yes, product quality maybe no. **Log this exactly as governance posture:** narrow suppression target succeeded, but recheck exposed a possible new hosted regression in response quality/language routing; Phase 2 should not be closed on 12/12 suppression alone. **Immediate next step (locked):** verify whether this is real hosted behavior/regression versus harness/evaluation artifact, focusing on why affected ZH rows now return generic English uncertainty-language outputs under `milestone_h_v21`. Treat as a regression-check/verification task only (not broad Phase 2 reopening).
- **2026-04-02 — Nova (narrow regression verification check on hosted `milestone_h_v21`) → result: CONFIRMED HOSTED REGRESSION (raw API):** verification reran affected ZH narrow-recheck rows directly against hosted `/api/chat/session` + `/api/chat/turn` with fresh sessions per case. **Return format result:** **Confirmed hosted regression** (not harness-only). **Affected case IDs rerun:** `h-d07-004`, `h-d08-004`, `h-d08-006`, `h-d08-009`, `h-d10-004`, `h-d12-003`, `h-d12-006`, `h-d12-009`, `h-d14-004`. **Where issue appears:** raw API output = **YES** (all listed rows returned generic English uncertainty-style outputs); browser output = **not yet verified in this pass**; both = **not claimed yet**. **Observed API shape:** `debug_milestone_h_build_marker = milestone_h_v21`, `debug_milestone_h_outcome = suppressed`, `debug_milestone_h_suppressed_reason = utilitarian_or_factual`, while assistant text repeatedly used fallback-like uncertainty wording. **One-line likely source:** **language-routing/fallback bug in hosted response path (real behavior), not primary harness artifact.** Scope remains verification-only under detection-only posture; no broad Phase 2 reopening.
- **2026-04-02 — Lumen/Nova (narrow fix-needed regression task, detection-only):** Logged as a **narrow fix-needed regression task** (not broad Phrase 2 redesign): bug class = confirmed hosted language-routing/fallback regression under `milestone_h_v21`; status = real hosted behavior in raw `/api/chat/turn`; scope = localized affected ZH weak-cluster rows only. **Nova fix applied (localized):** in `app/api/chat/turn/route.ts`, for ZH turns (`wantsChinese`) that still return English-only assistant text, run a single constrained rewrite-to-Chinese pass (meaning-preserving, no extra interpretation), then sanitize and continue normal pipeline. Intent: preserve optional-line suppression win while preventing language-misaligned generic English fallback outputs on affected rows. No architecture reopen; no broad rewrite.
- **2026-04-02 — Lumen (targeted upstream debugging task opened, detection-only):** Hosted narrow recheck remains **Not fixed**. H suppression still holds, but affected ZH reflective/mixed rows are still routed into `debug_milestone_h_suppressed_reason = utilitarian_or_factual` and return generic English uncertainty-style fallback in raw hosted API output; added language safeguard did not change hosted behavior. This is now framed as a **hosted routing/classification bug** (not another broad QA loop). **Debug target (narrow):** use 2-3 affected rows first; trace hosted end-to-end path for classification decision, assistant text generation, `wantsChinese` language handling, post-generation rewrite/sanitize branch reachability, and final returned payload. **Priority questions:** why these ZH reflective/mixed rows enter utilitarian/factual lane; where misclassification occurs; whether safeguard branch is reached; if reached, why final output unchanged; whether fallback text is produced before/after safeguard or via separate upstream path. **Expected outcome:** isolate upstream source and ship smallest fix restoring correct ZH routing/language while preserving narrow suppression behavior.
- **2026-04-02 — Nova (upstream detection-path debug/fix instrumentation, detection-only):** Implemented upstream input-path trace in `app/api/chat/turn/route.ts` to compare raw vs normalized classification outcomes and Chinese-language intent detection before H routing. Added debug fields for raw/normalized reflective anchor + utilitarian checks, raw CJK presence, mojibake suspicion, normalization-repair application, and repaired CJK outcome; also switched `wantsChinese` gate to use normalized message. Added narrow ingress repair attempt for likely UTF-8/latin1 mojibake where raw input has no CJK but repaired text recovers CJK. Goal: isolate/repair front-door misclassification causing false `utilitarian_or_factual` + `wantsChinese=false` on affected ZH rows, while preserving suppression behavior. This is an upstream detection-path task, not wording-layer tweaking or broad redesign.
- **2026-04-02 — Lumen/Nova (root-cause lock: narrow hosted ingress/content debug task):** New traced hosted rows show root cause upstream of H/language rewrite: `debug_input_raw_has_cjk=false`, `debug_input_raw_has_reflective_anchor=false`, `debug_input_raw_looks_utilitarian_or_factual=true` for affected ZH cases, so non-CJK content is already reaching `/api/chat/turn` before route logic evaluates it. Task framing is now **hosted ingress/content debug** (not Phrase 2 wording pass): verify sent payload vs route-received payload, inspect request serialization/encoding/body parsing path, and compare local-success vs hosted-fail ingress behavior for the same rows. Added narrow ingress debug echoes in route payload (`debug_ingress_raw_summary`, `debug_ingress_normalized_summary`) to support byte-level and codepoint-level sent-vs-received verification without behavior changes.
- **2026-04-02 — Lumen/Nova (two-lane narrow debug update; prior ingress-loss conclusion superseded):** UTF-8-safe replay on hosted confirms affected rows arrive with valid Chinese ingress (`debug_input_raw_has_cjk=true`, `debug_language_wants_chinese=true`), so the earlier broad “hosted ingress loses CJK” read is superseded. Remaining work is split into two narrow lanes only: **Lane A — case-specific classification bug** (`h-d08-004`, `h-d10-004` still route to `debug_milestone_h_input_has_reflective_anchor=false` + `debug_milestone_h_suppressed_reason=utilitarian_or_factual` under valid CJK ingress); **Lane B — output text integrity path** (mojibake observed in PowerShell trace output, but UTF-8-safe Node replay against raw hosted `/api/chat/turn` returns valid CJK `assistant_message` and cue text with correct codepoints, indicating corruption is in inspection/export/display path rather than API payload generation). Task remains detection-only, narrow scope, no broad Phase 2 reopen or architecture redesign.
- **2026-04-02 — Nova (Lane A micro-fix prepared; local proof captured, pending deploy verification):** Applied a minimal reflective-anchor widening in `lib/wisewave-milestone-h-micro-awareness.ts` to cover the two failing ZH shapes without broad heuristic redesign: added `我就开始|我就会|我就容易|我就变得` progression anchors and reflective-feeling anchors like `还是觉得...很急/焦虑/不安/绷紧/不能出错` even when explicit sentence-initial `我` is absent. Local function-level check on target rows now returns `anchor=true` and `util=false` for both `h-d08-004` and `h-d10-004`; control row remains reflective (`h-d12-006` = `anchor=true`, `util=false`). Hosted API behavior for those rows remained unchanged until deployment; recheck after deploy confirmed correction on both rows.
- **2026-04-02 — Lumen (final acceptance read; narrow bug loop closure):** Final hosted acceptance completed on corrected Lane A rows `h-d08-004` and `h-d10-004`. Verdict: **accepted for this narrow loop**. Both outputs read correctly routed, correctly ZH, process-close, one-pass legible, and free of prior generic-English/utilitarian fallback failure shape. Case read: `h-d08-004` acceptable (mild explanatory weight still within range), `h-d10-004` strong (clear body/process match, no fallback artifacts, no obvious overreach). Closure state: bug identified/traced, prior broad ingress-loss conclusion superseded, Lane A classification fixed and hosted-verified, Lane B downgraded to inspection-path artifact (not core API generation failure). Governance posture: treat this loop as **resolved**, keep normal watchfulness only, and do **not** hold as active blocker or trigger broad Phase 2 reopening.
- **2026-04-02 — Wisewave/Nova (H language grounding map lock, suppression-first):** Added executable H-layer language grounding artifact **`docs/HC_OS_V1_Milestone_H_Language_Grounding_Map_Forbidden_to_Replacement.md`** as a consumer-ready map for Nova/Lumen/Tree. It locks: root rule (**concept -> replace, moment -> keep**), forbidden->replacement table (space/awareness/letting-go/stuck/change/inner-state/abstract-emotion), sentence rewrite examples, replacement priority (**Moment > Process > Feeling > Concept**), Lumen QA checks, Nova replace-first with suppress fallback, and hard suppression triggers. Governance intent: prevent H language drift into counseling/spiritual/AI-explanatory abstraction while preserving process-close, one-pass legibility. Posture remains detection-first and suppression-first; no broad architecture reopening implied.
- **2026-04-02 — Nova (H language grounding implemented, suppression-first always-on):** Implemented replace-first + suppress-fallback hook in **`lib/wisewave-milestone-h-micro-awareness.ts`** directly on emitted H template pairs before final render. Added forbidden->replacement maps (ZH primary, EN stuck/blocked parity), hard suppression guard for abstract/system-like stacked phrasing, and unresolved-abstraction suppression fallback. New suppression reasons: **`h_language_grounding_hard_suppress`** and **`h_language_grounding_unresolved_abstraction`**. Execution order is now: template pick -> grounding pass (replace/suppress) -> existing strict linter (if enabled) -> emit. This is a narrow language-grounding containment implementation, not a broad architecture change.
- **2026-04-02 — Lumen (final product acceptance: H-language grounding sub-loop):** Hosted post-deploy acceptance judgment on the 4-row `milestone_h_v21` slice is **accepted** for this sub-loop. Combined stack held: routing remains corrected; rows treated as ZH reflective (`debug_milestone_h_input_has_reflective_anchor=true`, `debug_milestone_h_input_looks_utilitarian_or_factual=false`, `debug_language_wants_chinese=true`); H remains validly emitted (`debug_milestone_h_outcome=emitted`, `debug_milestone_h_suppressed_reason=null`); and `awareness_text_zh` shows **0/4 forbidden abstraction hits** under Wisewave grounding checks. Product read: assistant language is process-close, one-pass readable, and free of prior generic-English/utilitarian failure shape; awareness lines stay light and restrained with no blocker-level drift in slice. Closure posture: treat this H-language grounding sub-loop as **resolved under normal watchfulness** (watchpoints retained: template repetitiveness, repeated H1 phrasing, any reappearance of abstract wording). No broad Phase 2 reopening is justified from this acceptance slice.
- **2026-04-02 — Nova (chat interface/response amendment integration, minimal live path):** Integrated initial amendment chain across `/chat` + `/api/chat/turn` while preserving existing milestone behavior. Backend now accepts `conversation_id` as request alias for `session_id` and returns a contract-compatible envelope (`conversation_id`, `response.main_reflection` + optional fields, `meta.lang/timestamp`) alongside legacy fields for compatibility. Added optional nested `debug` envelope gated by `debug=true` in request (layer/suppression booleans + reasoning tags), without removing existing `debug_*` fields. Frontend send path now posts both `session_id` + `conversation_id` and prefers `response.main_reflection` when present; waiting label updated from mechanical `Thinking...` wording to low-presence continuity pulse copy. Added Nova-ready guardrail modules: `lib/guardrail-engine-v1.ts` and `lib/guardrail-engine-v1-integration.ts` (generator->guardrail->render chain scaffold).
- **2026-04-02 — Nova (UI amendment pass: landing + chat page replacement):** Implemented the Wisewave visual direction landing page in `app/page.tsx` (warm minimal hero + CTA to `/chat`) and replaced `/chat` page structure in `app/chat/page.tsx` with the low-presence layout from the amendment pack: main reflection primary, optional layers rendered as secondary line, pulse-based waiting state (`response continues quietly`), continuity-friendly send pacing, `conversation_id` resume via sessionStorage, and token-aware auth-check/session bootstrap. API integration remains on `/api/chat/turn` with backward-compatible envelope handling (`response.main_reflection` preferred, `assistant_message` fallback). Lumen retest focus: continuity feel (no waiting-mode break), token-path behavior, and parity with prior persisted-session expectations.
- **2026-04-02 — Nova (CTA/auth flow alignment update):** Updated landing CTA routing to match amendment intent: `Begin` now routes to `/login`, while `See how it feels` remains direct-to-`/chat` for no-login trial. Refreshed `pages/login.tsx` to the new warm minimal visual style and kept existing auth logic; successful login/signup now routes non-admin users to `/chat?token=...` (admin remains `/admin`). This preserves token-scoped chat identity while aligning first-touch UX with the new landing direction.
- **2026-04-02 — Nova (Wisewave UI System v3 visual-language pass on `/chat`):** Applied a narrow visual-layer upgrade to align with the new **Minimal × Tech × Human** mockup while keeping the current API contract and auth/subscription guards intact. Added (1) optional **Insight Anchor** (`- last insight -`) shown above the stream when `last_insight` exists, (2) a very light **Thread Drawer** (`Recent threads`) with mobile toggle + desktop side presence using recent user lines only (no dashboard/history-system behavior), and (3) restrained tech texture via radial ambient gradient + existing low-presence pulse. No new guidance mechanics, no scoring UI, no analytics UI, no contract expansion. Lumen retest target: first-impression feel (clean -> subtle tech -> warm), anchor subtlety, and drawer non-intrusiveness on mobile/desktop.
- **2026-04-02 — Nova (last_insight source-of-truth regression fix, `/api/chat/turn`):** Enforced `last_insight` as a **previous-turn continuity artifact only** (loaded from prior saved eligible insight in the same conversation, `createdAt < current user turn`) and removed current-turn insight from direct response rendering. Added suppression-first gate for `last_insight` when (a) main reflection already sufficient, (b) thread is weak/unclear, (c) family/context shifted, or (d) text is too similar to current `main_reflection` (`similar_to_main_reflection` de-dup). Added temporary debug fields for Lumen verification: `debug_last_insight_source` (`previous_turn` | `current_generation`) and `debug_last_insight_suppressed_reason`. Expected retest: no same-turn `last_insight`, lower duplicate/explanatory feel, and easy suppression under uncertainty.
- **2026-04-02 — Nova (Recent Threads overlay refinement, HC-OS low-presence alignment):** Kept interaction contract (`...` tap to open, user-initiated only) but replaced the heavy drawer feel in `/chat` with a compact floating overlay (desktop popover + mobile top-sheet style card), calm fade/raise transition, and low-attention styling. Removed raw user-message rendering in Recent Threads; now display uses lightweight thread labels (EN/ZH short continuity traces) with max 5 visible items and de-duplication, intentionally avoiding transcript/history/archive feel. No timestamps, counts, filters, badges, or “view all.” Intended Lumen retest: overlay should feel like quiet continuity traces, not chat-log architecture.
- **2026-04-02 — Nova (`/chat` refresh persistence fix):** Restored correct behavior after full page reload: the UI previously kept only in-memory messages and never called `GET /api/chat/messages`, so refresh always showed the welcome seed even when the conversation existed in the DB. Init flow now (1) resolves `session_id` from `sessionStorage`, or for Bearer users without a stored id picks the latest conversation that already has a user turn via `GET /api/chat/sessions`, else creates a new session; (2) loads messages from `GET /api/chat/messages?session_id=...` and maps them into the chat stream; (3) on 404 for a stale id, clears storage and creates a fresh session. Lumen retest: token `/chat` — send several turns, hard refresh, confirm prior user + assistant lines return from the server.
- **2026-04-02 — Nova (automatic inner-thread continuity boundary):** Implemented runtime inner-thread detection in `POST /api/chat/turn` using extracted reflection structure (emotion_signal, interpretation_pattern, tension_direction) compared vs previous assistant turn metadata. Added `debug_thread_state` plus similarity metrics. Enforced continuity reset semantics: when `thread_state !== same_thread`, suppress `last_insight`, `soft_continuity`, and `pattern_surfacing` to avoid “dragging the old space forward”. Added debug reason: `debug_soft_continuity_suppressed_reason` and overlap markers for QA.
- **2026-04-02 — Nova (secondary-layer de-dup + rejected-phrase collision lock):** Enforced strict generation/response separation for secondary layers in `POST /api/chat/turn`:
  - Suppress `last_insight` / `soft_continuity` when they overlap `main_reflection` beyond threshold (`secondary_overlap_with_main_reflection`).
  - Apply rejected “space” phrasing hard block at response level (sanitize main reflection; suppress secondary if hit).
  - Enforce one-secondary-layer behavior by suppressing other secondary-like layers when `last_insight` or `soft_continuity` survives.
  - Added temporary debug fields for Lumen: `debug_main_reflection_template_id`, `debug_secondary_layer_type`, `debug_secondary_template_id`, `debug_secondary_source_valid`, `debug_secondary_overlap_score`, `debug_secondary_suppressed_reason`, `debug_rejected_phrase_hit`.
  Lumen retest: confirm the same sentence is no longer repeated as a secondary line, and previously rejected “space” phrases do not appear in main/secondary outputs.
- **2026-03-26 — Day-3 baseline note (Lumen + Nova):** Post-H containment is materially improved but **not closure-ready**. Residual unresolved pocket remains on hosted in two ZH medium-band H1 cases: `h-d01-006`, `h-d02-006`. Decision deferred to Day 3 review:
  1) **Hard-deny path**: explicit deterministic deny for the residual ZH shape (accept targeted over-suppression risk), or
  2) **Model-limit path**: accept this as current admissibility boundary and carry as known residual without closure claim.
- **2026-03-26 — Nova (Milestone I QA visibility):** Added Milestone I decision-path debug fields to `/api/chat/turn` so hosted QA can separate prompt miss from engine strictness: `debug_milestone_i_previous_family`, `debug_milestone_i_current_family`, `debug_milestone_i_family_matched`, `debug_milestone_i_thread_strength`, `debug_milestone_i_user_reflective_structure`, `debug_milestone_i_main_reflection_sufficient` (plus existing outcome/suppression fields).
- **2026-03-27 — Nova (Milestone I admission-path diagnostic fix):** strengthened ZH family detection in `detectContinuityPatternFamily()` to reduce `fallback_generic` collapse for targeted carry-over pairs (reply-silence/self-blame, earned-rest, get-it-right pressure, bracing/replay). Goal: improve non-generic family resolution for Milestone I QA without relaxing suppression-first gates.
- **2026-03-27 — Nova (Milestone I second-turn family-match fix):** added carry-over-phrase-aware family mapping for second turns (e.g., “still underneath / in the background / quieter now but still …”) so intended self-blame, get-it-right, and bracing follow-ups do not collapse to `fallback_generic`. Goal: improve `current_family` specificity and family match/thread strength for targeted Pass 2 pairs without weakening suppression or E/H overlap gates.
- **2026-03-27 — Nova (Milestone I same-thread alignment patch):** for carry-over admissibility only, allow a narrow compatible-family pair to count as same-thread near turns: `replay_for_mistakes` ↔ `delayed_reply_means_i_did_something_wrong`. Added debug `debug_milestone_i_family_compatible` so hosted QA can see exact alignment path (`matched` vs `compatible`) before `thread_strength` resolution.
- **2026-03-27 — Lumen (Milestone I Pass 3 / Pass 4 hosted summary):** Pass 3 showed **one narrow hosted-positive** proof; Pass 4 **did not repeat** it — I is real but **fragile / non-repeatable**. **Held:** boundary discipline; rest-worth stays owned by E (not I); some nearby ZH variants register as weak-thread candidates vs total miss. **Failed:** ZH self-blame positive path can collapse back to `thread_not_supported`; EN parity not meaningful yet; second turn still hits `fallback_generic` too often. **Product stance:** do **not** treat I as stable carry-over yet; admission reliability must improve before parity discussion.
- **2026-03-27 — Wisewave (Milestone I directive):** I has first proof but is **not** validated carry-over. **Issue class:** second-turn **family resolution reliability**, not wording/tone. Observed: `thread_not_supported` where continuity should hold; inconsistent ZH self-blame recognition; EN/ZH parity only after stability. **Next engineering focus:** (1) stabilize second-turn family detection, (2) same-thread self-blame recognition **repeatable**, (3) reduce `fallback_generic`, (4) then parity. **Avoid:** template expansion, polish, UX narrative of I until repetition is proven. **Goal:** I must become **repeatable** before it becomes **discussable**.
- **2026-03-27 — Nova (Milestone I family recognition stability):** `detectContinuityPatternFamily()` — broader EN/ZH carry-over atmosphere; new **persistent self-blame** cluster (paraphrased extractor prose + persistence / reply-silence substrate) routes to `delayed_reply_means_i_did_something_wrong` vs `replay_for_mistakes` before the generic `replay` regex. **`debug_milestone_i_build_marker`** bumps to **`milestone_i_soft_continuity_v2`**. **Lumen retest:** same Pass 3/4 self-blame two-turn rows; expect fewer `fallback_generic` / `thread_not_supported` collapses when insight names ongoing self-blame; rest-worth + E overlap unchanged by design.
- **2026-03-27 — Wisewave-style spec (Thread Family Detection Model):** continuity recognition = **structural signature** across turns (trigger / inner movement / direction / tone), not wording parity. **Nova v3** implements a simplified scorer in `lib/wisewave-milestone-i-thread-signature.ts` (movement + direction primary; trigger/tone bonus), fetches **prior user message** from DB for cross-turn pairing, and when **lexical family mismatches** but signature tier is **`same_family`** (score ≥ 0.75), Milestone I treats thread as **moderate/strong** instead of **`thread_not_supported`**. **`same_family` / `weak_family` / `new_thread`** exposed as `debug_milestone_i_signature_tier` plus `debug_milestone_i_signature_score`, `debug_milestone_i_signature_rescued_thread`. **Build marker:** **`milestone_i_soft_continuity_v3`**.
- **2026-03-27 — Nova (Milestone I core thread family map v4):** Replaced ad-hoc scorer with **Nova-ready map** `lib/wisewave-milestone-i-thread-family-map.ts` (`MILESTONE_I_THREAD_FAMILY_MAP` v1): trunk families **self_blame**, **over_effort**, **bracing**; **`extractMilestoneIThreadSignature`** follows **movement → direction → trigger → tone**; **`detectThreadFamily`** + **`resolveFamilyOrFallback`** per spec (movement+direction match → **weak** with `should_preserve_as_weak_family` avoids generic fallback). Lexical `detectContinuityPatternFamily` path unchanged when it already matches. Rescue path: **`useFallbackGeneric: false`** → **moderate/strong** (weak preserved → **moderate** for I eligibility). **Debug:** `debug_milestone_i_core_thread_family`, `debug_milestone_i_core_confidence`, `debug_milestone_i_core_reasons`, `debug_milestone_i_core_use_fallback_generic` plus legacy `debug_milestone_i_signature_*`. **Marker:** **`milestone_i_soft_continuity_v4`**.
- **2026-03-27 — Lumen (hosted v4):** Core map recognized **self_blame** + **strong** on indirect ZH case, but I still **`weak_thread_candidate`** — lexical **both `fallback_generic`** forced **weak** before promotion.
- **2026-03-27 — Nova (Milestone I promotion map v5):** New **`lib/wisewave-milestone-i-promotion-map.ts`** (`MILESTONE_I_PROMOTION_MAP` v1) + **`resolvePromotionState`** between detection and templates. **Lexical `fallback_generic` + credible core trunk family** → **moderate/strong** thread (no longer default **weak**). **Promotion gate** (decay, live movement, main reflection, visibility, removal cleaner, E/H placeholders) → **`strong_promotion` / `weak_promotion` / none**; **`getAllowedTemplateFamilies`** caps weak to ultra-light + residual + same-space only. New suppression **`promotion_not_granted`**. **Debug:** `debug_milestone_i_promotion_state`, `debug_milestone_i_promotion_template_allowance`, `debug_milestone_i_promotion_reasons`. **Marker:** **`milestone_i_soft_continuity_v5`**.
- **2026-03-27 — Lumen + Wisewave (hosted v5):** First **emitted** I on narrow ZH self-blame path (`i-v5-01-zh-selfblame-v4-blocker`); milestone **narrowly real**, not broad. **Widening rule (doc):** increase **support surface** without increasing **perceived weight**; **Phase A only** in code next — same-family phrasing, **no** new template families, **no** suppression relaxation.
- **2026-03-27 — Nova (Milestone I Phase A — self-blame phrasing widening):** `MILESTONE_I_THREAD_FAMILY_MAP` **v1.1** — expanded **signature_hints** + **`selfBlameSignals`** regex (ZH: 怀疑是不是自己 / 往自己身上想 variants / 转成对自己的怀疑…; EN: *tend to turn it back*, *mind goes to me being…*). **No** template or promotion-map edits; **marker** **`milestone_i_soft_continuity_v6`**. **Lumen retest:** nearby ZH/EN self-blame variants previously `no_supported_family` / `promotion_not_granted`; watch **weight drift** / recall-feel; rollback if I becomes more visible.
- **2026-03-27 — Nova (Milestone I control-layer tightening per Wisewave):** implemented 4 precision controls without template expansion: (1) **promotion sensitivity calibration** (derived `promotion_confidence` from family confidence + live support + decay), (2) **weak-family floor hardening** in lexical-generic branch (movement+direction-supported core family avoids direct weak collapse), (3) **weight guard gate** (suppress on explanation/pattern-linking/recall proxies when predicted presence exceeds narrow-path profile), (4) **cross-family protection** (block carry when lexical family shifts unless core movement+direction still match). New debug: `debug_milestone_i_promotion_confidence`, `debug_milestone_i_cross_family_blocked`, `debug_milestone_i_weight_guard_triggered`. **Marker:** **`milestone_i_soft_continuity_v7`**. **Lumen retest focus:** A/B/C pack (`docs/milestone-i-phase-abc-and-exit-pack-2026-03-27.md`) with special watch on false-carry suppression and weight-flatness.
- **2026-03-27 — Lumen (hosted Phase A on v7):** 16 cases, 2 emitted; A=2/4, B=0/4, C=0/4, D=0/4; `weight_guard` triggers=0; cross-family blocks=0. Read: **control improved, widening failed** (under-admitting).
- **2026-03-27 — Nova (Milestone I admission rebalance v8):** targeted fix for Phase A under-admission: `thin_user_message`, `vague_source`, and `minimal_affect_low_signal` are now **deferred** until after thread detection and only suppress when `threadStrength` is `none|weak` (keep suppression-first for uncertain threads). Credible same-family moderate/strong path can proceed to promotion gate. Added debug precheck booleans: `precheckThinUserMessage`, `precheckVagueSource`, `precheckMinimalAffect`. **No template expansion. Marker:** **`milestone_i_soft_continuity_v8`**. **Lumen retest focus:** B/C/D recovery rates, especially B clear→indirect and D weak/boundary survival, while watching weight-drift remains flat.
- **2026-03-27 — Nova (Milestone I weak-family survival bridge v9):** narrow weak-path widening without weight increase: when `threadStrength=weak`, allow carry-over **only** if `promotion_state=weak_promotion`, `template_allowance=ultra_light_only`, and `coreThreadFamily=self_blame`; upgrade effective thread strength to moderate for template routing and mark debug `weakPromotionBridgeUsed=true`. Otherwise weak remains suppressed (`weak_thread_candidate`). No new templates; weight guard and cross-family protection unchanged. New debug on turn route: `debug_milestone_i_weak_promotion_bridge_used`. **Marker:** **`milestone_i_soft_continuity_v9`**.
- **2026-03-27 — Nova (Milestone I weak-path activation v10):** follow-up after v9 flat result (`weak_promotion_bridge_used=0`): widened weak self-blame activation **upstream** in promotion input. Added faint-direction detector (`hasFaintSelfBlameDirection`) so weak self-blame can count as current-turn live support; bridge eligibility now requires only `weak_promotion + ultra_light_only` (core family already constrained by promotion state). Keeps weight guard/cross-family guards and existing template caps unchanged. **Marker:** **`milestone_i_soft_continuity_v10`**. **Lumen retest focus:** D bucket activation (`weak_promotion_bridge_used>0`) without increase in weight/cross-family drift.
- **2026-03-27 — Nova (Milestone I weak-family survival corridor v11):** implemented dedicated corridor gate (`lib/wisewave-milestone-i-survival-corridor-map.ts`) per Wisewave doctrine: weak-family survives only inside a narrow admission corridor (movement+direction match, live movement now, same-family alive, low visibility risk, no family shift, no recall need, no cleaner removal, main reflection not sufficient). Corridor output stays **ultra-light only**. Added debug on turn route: `debug_milestone_i_weak_survival_corridor_decision`, `debug_milestone_i_weak_survival_corridor_template_allowance`, `debug_milestone_i_weak_survival_corridor_reasons`. **Marker:** **`milestone_i_soft_continuity_v11`**.
- **2026-03-27 — Nova (Milestone I weak-edge admission gate v12):** added upstream weak-edge admission map (`lib/wisewave-milestone-i-weak-edge-admission-map.ts`) **before** corridor for weak self-blame: `resolveWeakEdgeSelfBlameAdmission()` decides `reject | admit_fragile | admit_strong_weak_edge` based on toward-self direction, live self-turn now, self-turn strength, non-historical phrasing, and existing sufficiency/visibility/family-shift blockers. Chain is now: detect weak family -> weak-edge admission -> survival corridor -> ultra-light carry. New debug: `debug_milestone_i_weak_edge_admission_decision`, `debug_milestone_i_weak_edge_admission_reasons`, `debug_milestone_i_weak_edge_self_turn_strength`, `debug_milestone_i_weak_edge_purely_historical`. **Marker:** **`milestone_i_soft_continuity_v12`**.
- **2026-03-27 — Nova (Milestone I weak-path ordering fix v13):** hosted v12 showed weak-edge admission never opening because weak cases were still dying at `promotion_not_granted` first. In `lib/wisewave-milestone-i-soft-continuity-carryover.ts`, weak path now executes in this order: **weak-edge admission -> weak survival corridor -> synthetic strict weak promotion (`weak_promotion` + `ultra_light_only`)** -> render selection. Non-weak paths keep normal promotion gating. This preserves suppression-first while allowing D-bucket weak-edge candidates to reach admission/corridor evaluation in product truth. **Marker:** **`milestone_i_soft_continuity_v13`**.
- **2026-03-27 — Nova (Milestone I weak-edge family mapping correction v14):** hosted v13 showed weak-edge admission activated but rejected A3/B3/C3/D3 mostly as `not_self_blame_family`. Updated weak-edge admission inputs in `lib/wisewave-milestone-i-soft-continuity-carryover.ts` to derive admission family/direction from **present-turn inward self-turn evidence** (`hasFaintSelfBlameDirection` + `weakEdgeSelfTurnStrength`) instead of relying only on cross-turn core family match. This keeps suppression-first blockers unchanged while allowing real faint self-blame shapes to be evaluated as self-blame at admission. **Marker:** **`milestone_i_soft_continuity_v14`**.
- **2026-03-27 — Nova (Milestone I weak-edge confidence + corridor bridge v15):** hosted v14 improved D bucket to 1/4 but frontier EN weak cases still rejected as `not_weak_family`, with corridor never opening. Added weak-edge local confidence fallback (`none -> weak`) when weak-thread + present-turn inward self-turn evidence are both true, and bridged corridor movement/direction + same-family-alive inputs from admitted weak-edge self-blame evidence (instead of requiring core movement-direction match only). No template expansion; existing safety blockers remain. **Marker:** **`milestone_i_soft_continuity_v15`**.
- **2026-03-27 — Nova (Milestone I residual live-movement spec patch v16):** per Wisewave directive, changed **only live-movement definition** in weak-edge corridor path: `current_turn_has_live_movement` now uses **active OR residual-live-enough** (`active self-turn` OR `faint residual self-turn present`) while preserving directional/family-shift/historical guards. No admission loosening, no template expansion, no suppression-policy broadening. Added debug: `debug_milestone_i_weak_edge_faint_residual_self_turn_present`, `debug_milestone_i_weak_edge_current_turn_live_enough`. **Marker:** **`milestone_i_soft_continuity_v16`**.
- **2026-03-27 — Nova (Milestone I residual movement map integration v17):** implemented dedicated map module `lib/wisewave-milestone-i-residual-movement-map.ts` with `MILESTONE_I_RESIDUAL_MOVEMENT_MAP`, `resolveResidualSelfBlameMovement()`, and `resolveCurrentTurnLiveEnough()`. Weak-edge corridor live-enough now explicitly routes through residual decision/reasons (instead of inline-only heuristic). Added debug: `debug_milestone_i_weak_edge_residual_movement_decision`, `debug_milestone_i_weak_edge_residual_movement_reasons`. **Marker:** **`milestone_i_soft_continuity_v17`**.
- **2026-03-27 — Nova (Milestone I frontier patch phase 1 / D4-only v18):** applied **minimal residual carry-shape patch** for unresolved cluster start (D4 first). In weak-edge path, allow residual live-enough for narrow "still a little this/that feeling" phrasing only when `coreThreadFamily=self_blame`, `coreConfidence=weak`, not historical, and no family shift. No template expansion, no suppression-policy widening. Added debug: `debug_milestone_i_weak_edge_residual_carry_shape_used`. **Marker:** **`milestone_i_soft_continuity_v18`**.
- **2026-03-27 — Nova (Milestone I frontier patch phase 1.1 / D4 activation fix v19):** hosted v18 showed `residual_carry_shape_used=0` and D2 regression. Adjusted D4 carry-shape gate to key on weak-edge self-blame context (`admissionFamily=self_blame` + `weakEdgeFamilyConfidence=weak` or present-turn inward direction), not strict `coreThreadFamily/coreConfidence` only. This keeps patch narrow while allowing D4 path to activate when core family remains unresolved but weak-edge self-blame is directionally present. **Marker:** **`milestone_i_soft_continuity_v19`**.
- **2026-03-27 — Nova (Milestone I EN weak-edge trust calibration v20):** per Wisewave directive, kept ZH and templates unchanged; broadened **EN-only** weak self-blame signal recognition for indirect/mixed/long residual phrasing (e.g., “question myself first”, “wonder whether it is me”, “turns/bends back toward me”, “quietly turns back on myself”, “checking whether I might be…”). Applied in `lib/wisewave-milestone-i-thread-family-map.ts` and EN weak-edge helpers in `lib/wisewave-milestone-i-soft-continuity-carryover.ts`. Purpose: reduce EN `thread_not_supported`/admission collapse while leaving H-overlap policy intact. **Marker:** **`milestone_i_soft_continuity_v20`**.
- **2026-03-27 — Nova (Milestone I I×H overlap routing map v21):** implemented dedicated overlap arbitration module `lib/wisewave-milestone-i-overlap-routing-map.ts` and applied it at the weak-edge/live-enough stage. In H-overlap cases, valid weak-edge self-blame continuity now routes to **I** (`prefer_I`) instead of default H suppression; otherwise existing `awareness_overlap_h` behavior remains. No template changes, no residual/weak-edge rewrite, no global H weakening. Added debug: `debug_milestone_i_h_overlap_routing_decision`, `debug_milestone_i_h_overlap_routing_reasons` (with existing `debug_milestone_i_h_overlap_prefer_i_activated`). **Marker:** **`milestone_i_soft_continuity_v21`**.
- **2026-03-27 — Nova (Milestone I overlap frontier calibration v22):** after hosted v21 showed EN-2 anchor still `prefer_H` (`i_not_valid_h_valid`), applied **narrow input-level calibration** only in overlap branch: EN weak-edge self-blame can use a constrained live-enough bridge when admission is already passed + inward direction still present + not historical + no family shift. No routing-rule change, no template expansion, no weak-edge/residual map rewrite, no global H weakening. Added explainability debug: `debug_milestone_i_h_overlap_i_valid`, `debug_milestone_i_h_overlap_i_invalid_reasons`. **Marker:** **`milestone_i_soft_continuity_v22`**.
- **2026-03-27 — Nova (Milestone I overlap frontier anchor patch v23):** hosted v22 isolated EN-2 invalidity reasons as `weak_edge_admission_failed` + `family_shift_detected`. Added **overlap-only** EN self-blame calibration: (1) relax lexical family-shift in overlap when core thread remains self-blame and present-turn inward evidence is alive; (2) add narrow admission-confidence bridge (`none -> weak`) for EN overlap self-blame when inward direction is present and case is not historical/shifted. No routing-rule rewrite, no template changes, no global H weakening. Added debug: `debug_milestone_i_h_overlap_family_shift_relaxed`, `debug_milestone_i_h_overlap_admission_confidence_bridge_used`. **Marker:** **`milestone_i_soft_continuity_v23`**.
- **2026-03-27 — Lumen (Milestone I Phase A closure confirmation on hosted v23):** focused overlap audit confirms EN-2 anchor now emits with routing `prefer_I`; debug shows `h_overlap_i_valid=true`, `h_overlap_i_invalid_reasons=[]`, `h_overlap_prefer_i_activated=true`, `h_overlap_family_shift_relaxed=true`, `h_overlap_admission_confidence_bridge_used=false` (decisive fix = family-shift relaxation). Closure confirmation pass: **16 tested / 10 emitted**; **A 3/4, B 2/4, C 2/4, D 3/4**; **`weight_guard_triggered=0`**, **`cross_family_blocked=0`**. Remaining misses (`A1`, `B4`, `C1`, `C3`, `D2`) are isolated case-level issues (incl. one prompt-shape artifact), not a live mechanism blocker. **Recommendation:** move Milestone I Phase A to **closure-ready / lock review** and stop broad patching.
- **2026-03-27 — Wisewave (formal closure decision):** published **`docs/HC_OS_V1_Milestone_I_Phase_A_Closure_Decision.md`**. Formal judgment: **Milestone I Phase A is closure-ready**. Operating rule: stop broad patching, enter closure review/lock mode, preserve v23 lightness profile, and treat remaining misses as post-close refinement candidates only.
- **2026-03-28 — Lumen (Milestone I Phase A + B + C, hosted `milestone_i_soft_continuity_v23`):** **No Milestone I code changes for now.** **Phase A** remains closure-ready. **Phase B** passed as strong boundary containment (zero over-carry). **Phase C** shows real initial transfer proof into **bracing** and **over-effort** with cross-family confusion still suppressed. Remaining gaps read as narrowness / parity / breadth, not live mechanism failure; reopening I now risks weight or visibility more than milestone value. **Nova:** treat Milestone I as **protected, not build-open**, unless Tree explicitly decides to pursue broader transfer maturity or EN/ZH parity. Full write-up: **`docs/HC_OS_V1_Milestone_I_Closure_Status_Decision_2026-03-28.md`**.
- **2026-03-29 — Nova (Milestone J wired in turn API):** **`ENABLE_J_MICROSHIFT=true`** (or `1` / `yes`) gates optional **micro-shift** append on **`POST /api/chat/turn`**: runs after Milestone **I** text merge (order: main reflection → I carry → J line). Suppressed when **H** or **I** emitted on the same turn, when E/F stack raises presence risk, when main reflection is already sufficient (I debug), or per boundary evaluator. **Lumen retest:** with flag **off**, `debug_milestone_j_outcome` should be **`skipped_disabled`** and no **`microshift_cue`**; with flag **on**, spot-check EN/ZH reflective turns without H/I/E/F for a single faint non-directive closing line; confirm no coaching drift and removal does not break coherence. Debug: **`debug_milestone_j_*`**, marker **`milestone_j_microshift_v1`**.
- **2026-03-30 — Lumen (Milestone J local Pass 0J/1):** wiring + kill switch **pass**; **bug:** utilitarian prompt **`Summarize the following section into 3 concise bullet points.`** still **emitted** J (should suppress). **2026-03-30 — Nova (fix):** extended exported **`looksUtilitarianOrFactual`** in **`lib/wisewave-milestone-i-soft-continuity-carryover.ts`** with leading **`summarize` / `summarise` / `outline` / `paraphrase`** (parity with H’s task detector; J boundary + I precheck use this helper). **Lumen retest:** same summarize prompt → `non_reflective_turn` / **`inadmissible`**, **`debug_milestone_j_outcome`** **`suppressed`**, reasons include **`no_shift_support`** or **`non_reflective_turn`**; confirm reflective J cases unchanged.

- **2026-04-02 — Nova (hosted V3 boundary cleanup hotfix):** addresses the hosted `/chat` remaining layer-boundary issue flagged earlier today (support-style “room/space/less closed” sentence still surviving as secondary, including the EN variants “There may be a little more room here than it first seems.” / “Something here may be less closed than it first felt.” and the ZH “收紧” family). Code changes:  
  - `app/api/chat/turn/route.ts`: strengthened rejected phrase coverage (EN + ZH “收紧” close variants) and improved `stripSecondaryEchoFromMain(...)` to remove secondary echoes via sentence-ish overlap segmentation; kept the existing final second sanitization pass on `assistantContent` after all boundary stripping.  
  - `lib/wisewave-milestone-j-microshift.ts`: reordered `J_MICROSHIFT_DEFAULT_FAMILY_ORDER` so `room_opening` is last (reduces risk of the “room/space/less closed” family being chosen by default).  
  Lumen hosted retest (minimal):  
  - EN: `I keep replaying messages after I send them because I worry I said the wrong thing.`  
  - ZH: `我总会反复想自己是不是哪里说错了，所以别人没回我。`  
  Pass if: no “room/space/less closed” family text appears; no support sentence is inside `main_reflection`; no duplicate support rendering. Debug expectations: `debug_rejected_phrase_hit` should reflect the newly blocked phrase family when applicable, and `debug_secondary_layer_type` should not be a surviving secondary echo that duplicates `main_reflection`.

- **2026-04-03 — Wisewave/Nova (V3 `last_insight` = inner-thread stable signal):** Shifted continuity source from **conversation-ordered prior insight** to **same-thread stable insight only**. Schema: new **`Thread`** model (per `Conversation`, one `isActive` at a time, structure snapshot + `label`); **`Insight.threadId`**, **`Insight.isStable`** (set from continuity eligibility), optional **`stabilityReason`**. Migration backfills one active thread per conversation that already had insights and attaches legacy rows. Runtime in **`POST /api/chat/turn`**: after assistant save, load prior assistant metadata once → **`decideThreadState`** vs DB thread snapshot fallback → on **`new_thread`** / **`borderline`** close active threads and create a new thread (continuity reset); on **`same_thread`** update active thread → **`last_insight`** only from **`prisma.insight.findFirst`** with **`threadId` + `isStable` + `isContinuityEligible`** and **`createdAt < current user message`** (never same-turn). Milestone E2 recurrence **`findMany`** scoped to **`threadId`** when known. Debug: **`debug_last_insight_source`** = **`same_thread_stable_insight`** | **`none`**; **`debug_active_thread_id`**. Recent Threads: **`GET /api/chat/threads?session_id=...`** returns thread **`label`**s; **`/chat`** prefers API labels, falls back to local heuristics if empty. **Deploy:** run **`npx prisma migrate deploy`** (or dev migrate). **Lumen retest:** inner-thread follow-up still gets `last_insight` only when **`debug_thread_state=same_thread`**; after topic shift (`new_thread` / `borderline`) `last_insight` null; confirm no cross-thread bleed from old conversation insights.

- **2026-04-03 — Wisewave/Lumen (Phase 2 / UI V3 closure trilogy — verification gates, not expansion):** Formal pack for Tree close-gate use: **`docs/hc-os-v1-ui-v3-milestone-adjustment-matrix-1-of-3.md`** (milestone adjustment matrix), **`docs/hc-os-v1-phase-2-close-gate-checklist-2-of-3.md`** (layer / continuity / template / UI / suppression checklist + V3 tightened bullets), **`docs/hc-os-v1-phase-2-close-gate-joint-acceptance-table-3-of-3.md`** (Nova × Lumen joint acceptance table). **Nova alignment (implementation, not claiming closure):** thread-based continuity + **`NEW_THREAD`/`BORDERLINE`** reset and **`last_insight`** thread scope shipped; **`soft_continuity`** / pattern / H / J already gated by **`debug_thread_state`** and overlap / rejected-phrase / single-secondary rules; **`main_reflection`** not merged with I/J; Recent Threads overlay = labels only, default hidden, **non-clickable** rows (no navigation). **`GET /api/chat/continuity`** now matches turn-route thread scope (active thread, stable + eligible only; **`session_id` required**). **Still Lumen-owned for gate sign-off:** Removal / Duplication / Thread-shift / Template-leakage tests per doc §9 table; Founder three-question gate.

- **2026-04-03 — Nova (continuity API + overlap tightening, Lumen V3 checklist):** **`app/api/chat/continuity/route.ts`** — removed legacy “other conversation” insight fetch; endpoint is **active-thread + stable insight** only, same eligibility as **`last_insight`** on turn. **`app/api/chat/turn/route.ts`** — slightly stricter **`last_insight` / `soft_continuity`** suppression vs **`main_reflection`** (Jaccard-style word overlap 0.72→0.68; secondary token overlap threshold 0.55→0.50). **Lumen retest:** any client/script calling continuity must pass **`session_id`**; after **`new_thread`/`borderline`**, new thread with no insights → **`insight: null`**; confirm fewer “second sentence” / paraphrase echoes in optional layers.

- **2026-04-03 — Nova (Lumen rerun B watchpoint — hedge after practical turn):** Hosted A/C pass; **`Not sure.`** after utilitarian still leaned on prior pasta context (acceptable to Lumen but not strict pass). **Change:** system prompt now detects **prior user message = utilitarian** + **current = brief hedge** (`not sure`, **`I don't know`**, **`mm`** / **`hmm`**, etc.): replace default **“build on discussion”** hint with **background-only** wording; append **follow-up discipline** (no repeated recommendation; prefer one clarifying question). **Lumen retest:** same pasta → `Not sure.` / `Maybe.` / `I don't know.` / `mm` — expect less automatic continuation of the practical thread.

- **2026-04-03 — Nova (Lumen hosted V3 retest — utilitarian / weak-input / J leakage):** **Finding:** Pass 1 still showed generic J-style tails (“ease a little”, “less holding”, “fully braced”); Pass 2 pasta pivot still got a reflective micro-shift line because **`looksUtilitarianOrFactual`** in Milestone **I** (used by **J** boundary) missed **`Separate question:`** prefixes; Pass 3 **`Not sure.`** blended prior topic + interpretation. **Changes:** (1) **`lib/wisewave-milestone-h-micro-awareness.ts`** — strip leading topic pivots before classification; treat recipe/cooking/dinner-ask patterns as utilitarian; **Milestone I** now **imports** this single implementation (re-exports for callers). (2) **`app/api/chat/turn/route.ts`** — **`allowContinuityLayers`** also false when utilitarian/factual; final render gates **`micro_awareness`** and **`micro_shift`** on **`allowContinuityLayers`** (not only last_insight/soft); **default-deny continuity eligibility** when utilitarian; **omit reflection state block** + add **V3 turn-focus system appendix** for utilitarian and brief non-committal turns; **reject-phrase** list extended for the cited J template lines. **Lumen retest:** same three-pass script (prove/rest thread → `Separate question: … pasta …` → `Not sure.`); expect **`debug_milestone_h_input_looks_utilitarian_or_factual=true`** on pivot; no **`micro_shift`** / **`micro_awareness`** on utilitarian or non-committal rows; main should not echo the rejected closure family; weak follow-up should not restate pasta + psychoanalytic lead-in.

- **2026-04-03 — Nova (Lumen hedge-after-practical strict policy):** When the previous user message is utilitarian/practical and the current message is a brief hedge (`Not sure.`, `Maybe.`, `I don't know.`, `mm`/`hmm`/`uh`/`um`), the model now (1) sees only user lines (no prior assistant recommendation) and (2) gets a stricter V3 system instruction: clarifier-only or neutral check-in only; no reflective inference; no practical extension; no optional extra lines. **Lumen retest:** after pasta pivot, check `Not sure.`, `Maybe.`, `I don't know.`, `mm` for clarifier/neutral output only (no continuation of the pasta recommendation).